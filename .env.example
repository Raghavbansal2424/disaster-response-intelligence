# Optional authorized dispatch gateway. Leave blank for safe simulation mode.
DISPATCH_WEBHOOK_URL=

# Store the real token only in your deployment environment. Never commit it.
DISPATCH_WEBHOOK_TOKEN=
